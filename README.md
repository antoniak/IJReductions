# IJReductions

## Compile

The code can be compiled with the provided makefile using the standard `make` command.

```
mkdir build
cd build
cmake ..
make all
```

## Execution
```
cd src
./IJReductions
```

## Test
